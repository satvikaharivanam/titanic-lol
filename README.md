using the dataset of titanic, which is a csv file, i trained a small model but processing, and then cleaning 
the data, the csv file. 
and then evaluated the model, and using a small python program, checked the working of the model, by asking the users to give 
inputs for parameters such as age, sex, class, and fare. etc. 
